# Scan existing tools before building modules

Each candidate module will get a lightweight existing-tool scan before we design our own implementation. We will adopt small healthy tools when they fit, wrap useful backends with awkward APIs, learn from unsuitable or abandoned attempts, and build only when the survey shows a real unmet need.
