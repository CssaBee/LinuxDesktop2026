# Drop binary plugin compatibility for the POC

For the first native Linux POC, we will not try to keep existing Windows plugin binaries working unchanged. That boundary keeps the editor milestone achievable and avoids freezing the design around the Win32 plugin ABI before the core Linux path exists.
