# Start platform libraries in a monorepo

The general-purpose platform libraries will start in one repository with separate modules, examples, tests, and shared build infrastructure. This makes survey-driven iteration and cross-module examples easier while the module boundaries are still being discovered.
