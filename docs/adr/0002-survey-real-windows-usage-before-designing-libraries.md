# Survey real Windows usage before designing libraries

Before designing the first reusable platform libraries, we will study a corpus of 15 to 20 repositories that use similar Windows desktop features, including about five projects with existing abstractions or ports. This keeps the libraries grounded in community requirements instead of overfitting them to Notepad++ alone.
